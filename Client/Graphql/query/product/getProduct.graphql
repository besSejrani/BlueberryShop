query GetProduct($productId: String!) {
  getProduct(productId: $productId) {
    _id
    name
    price
    description
    stock
    promotion
    status
    productImages
    reviews {
      reviewerName
      rating
      review
    }
  }
}
