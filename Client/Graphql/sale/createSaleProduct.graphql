mutation CreateSale(
  $sale: String!
  $startDate: DateTime!
  $endDate: DateTime!
  $discount:String!
  $productId: String!
) {
  createSale(
    saleInput: {
      sale: $sale
      startDate: $startDate
      endDate: $endDate
      discount: $discount
      productId: $productId
    }
  )
}
