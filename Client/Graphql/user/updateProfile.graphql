mutation UpdateProfile($picture: [Upload!]!, $username: String!, $email: String!) {
  updateProfile(picture: $picture, updateProfileInput: { username: $username, email: $email }) {
    _id
    username
    email
    profileImageUrl
  }
}
