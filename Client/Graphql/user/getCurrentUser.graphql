query getCurrentUser {
  getCurrentUser {
    _id
    username
    role
    profileImageUrl
    shipping {
      country
      address
      city
      zip
    }
    billing {
      country
      address
      city
      zip
    }
  }
}
